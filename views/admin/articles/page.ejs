<%- include('../../partials/header'); %>
<%- include('../../partials/homenavbar'); %>
<body>
   <div class="container">
        <% result.articles.rows.forEach(article => { %>
            <div class="card my-3">
                <div class="card-header">
                    <h3><%= article.title %></h3>
                </div>
                <div class="card-body">
                    <a href="/slug/<%= article.slug %>" class="badge badge-info p-3">Ler artigo</a>
                </div>
            </div>
        <% }) %>
        <div class="row">
            <div class="col col-12 d-flex justify-content-between">
                <% if (result.page > 2) { %>
                    <a href="/articles/page/<%= result.page - 1 %>"><<< Voltar</a>
                <% } else if (result.page == 2) { %>
                    <a href="/"><<< Voltar</a>
                <% } %>
                <% if (result.next) { %>
                    <a href="/articles/page/<%= result.page + 1 %>">Próximo >>></a>
                <% } %>
            </div>
        </div>
    </div>
</body>
<%- include('../../partials/footer'); %>